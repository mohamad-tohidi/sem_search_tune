model_name: "BAAI/bge-m3"
train_data: "data/train.jsonl"
eval_data: "data/val.jsonl"
loss: "mnrl"
batch_size: 2

# TODO: add gradiant acculmilation
gradiant_accumilation: 2
epochs: 10
lr: 2e-5
max_seq_length: 128
run_name: "parsa_001"
output_dir: null

dry_run: false
dry_run_samples: 64
dry_run_eval_samples: 16

benchmark:
  project: "parsa"
  k: 10
  query_prefix: "query: "
  passage_prefix: "passage: "
  data_dir: "benchmark_data"
